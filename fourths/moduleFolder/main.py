import example
# When you import a module, it runs everything inside the module, it just copies the module
# And it keeps it on top of here so it runs everything inside that module

result = example.circumference(3)

print(result)